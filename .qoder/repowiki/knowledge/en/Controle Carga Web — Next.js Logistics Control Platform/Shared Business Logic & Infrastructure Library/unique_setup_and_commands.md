ERP automation requires `ERP_AUTOMATION_ENABLED=1` plus an optional `ERP_AUTOMATION_PYTHON` path and `ERP_AUTOMATION_SCRIPT`; the spawned Python script lives at `scripts/erp_emitir_lista_separacao.py` relative to `process.cwd()` and must emit JSON on stdout. The `EtiquetaLote.cnpj` column may be absent in production until migration `20260807150000_add_cnpj_etiqueta_lote` runs — the repo probes `information_schema.columns` once per process to conditionally include it.