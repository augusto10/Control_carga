// Placeholder para evitar erro de build no Vercel
// Implemente a lógica de autenticação real conforme necessário
export default function useAuth() {
  return {
    user: null,
    login: () => {},
    logout: () => {},
    isAuthenticated: false
  };
}
